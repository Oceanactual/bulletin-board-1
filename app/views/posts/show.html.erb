<div>
  <div>
    <h1>
      Post #<%= @the_post.id %> details
    </h1>

    <div>
      <div>
        <a href="/posts">
          Go back
        </a>
      </div>

      <div>
        <a href="/delete_post/<%= @the_post.id %>">
          Delete post
        </a>
      </div>
    </div>

    <dl>
      <dt>
        Title
      </dt>
      <dd>
        <%= @the_post.title %>
      </dd>

      <dt>
        Body
      </dt>
      <dd>
        <%= @the_post.body %>
      </dd>

      <dt>
        Board
      </dt>
      <dd>
        <%= @the_post.board_id %>
      </dd>

      <dt>
        Posted on
      </dt>
      <dd>
        <%= @the_post.posted_on %>
      </dd>

      <dt>
        Expires on
      </dt>
      <dd>
        <%= @the_post.expires_on %>
      </dd>

      <dt>
        Created at
      </dt>
      <dd>
        <%= time_ago_in_words(@the_post.created_at) %> ago
      </dd>

      <dt>
        Updated at
      </dt>
      <dd>
        <%= time_ago_in_words(@the_post.updated_at) %> ago
      </dd>
    </dl>
  </div>
</div>

<hr>


<div>
  <div>
    <h2>
      Edit post
    </h2>

    <form action="/modify_post/<%= @the_post.id %>"  method="post" >
      <div>
        <label for="title_box">
          Title
        </label>

        <textarea id="title_box" name="query_title"><%= @the_post.title %></textarea>
      </div>

      <div>
        <label for="body_box">
          Body
        </label>

        <textarea id="body_box" name="query_body"><%= @the_post.body %></textarea>
      </div>

      <div>
        <label for="board_id_box">
          Board
        </label>

        <input type="text" id="board_id_box" name="query_board_id" value="<%= @the_post.board_id %>">
      </div>

      <div>
        <label for="posted_on_box">
          Posted on
        </label>

        <input type="date" id="posted_on_box" name="query_posted_on" value="<%= @the_post.posted_on %>">
      </div>

      <div>
        <label for="expires_on_box">
          Expires on
        </label>

        <input type="date" id="expires_on_box" name="query_expires_on" value="<%= @the_post.expires_on %>">
      </div>

      <button>
        Update post
      </button>
    </form>
  </div>
</div>

<hr>
